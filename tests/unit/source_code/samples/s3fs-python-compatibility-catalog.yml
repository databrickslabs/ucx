%YAML 1.2
---
identifier:
  name: leeds
  # All versions are compatible
top_level: leeds
packages:
  - name: leeds
    compatibility: full
    failures:
      - some failure 1
  - name: databricks.sdk.service.compute
    compatibility: full
    failures:
      - some failure 1
