[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "thingy"
version = "0.0.1"
description = 'This package does nothing useful and is meant for testing wheels.'

[project.entry-points.databricks]
runtime = "thingy:main"
